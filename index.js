// code your solution here
function superbowlWin(record){
    const win = record.find(record => record.result === "W")
    console.log("year", win)
        if(!win) {
            return win
        }
            return (win.year)
    
    
}
